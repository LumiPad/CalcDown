# Demo 3 — Agent Instructions

Scope: `demos/demo3/**`.

This folder showcases **tabular data input/output** with CalcDown `data` blocks and standardized `table`/`cards` views.

- Keep it dependency-light (static HTML + `dist/` output).
- Prefer CalcDown-standard `view` objects (`library: "calcdown"`).
- Follow `demos/AGENTS.md`: use `installCalcdownStyles()` and avoid restyling CalcDown component primitives in demo CSS.
